[workspace]
resolver = "2"
members = ["dbcli"]

[workspace.package]
version = "0.5.8"
edition = "2021"
license = "MIT OR Apache-2.0"
# std::os::fd / std File::lock (audit flock, issue #104) need 1.89+.
rust-version = "1.89"
