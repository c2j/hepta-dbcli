# Benchmark harness Python dependencies (venv-only; never added to Cargo.toml).
# Union of Case A (SDV) and P1 (SynMeter eval, CPU path — no torch in CI) needs.
# SynMeter upstream pins: scikit-learn 1.3.0, xgboost 2.0.3, catboost 1.2.2, POT 0.9.3.
sdv>=1.17,<2
sdmetrics
boto3
botocore
pandas>=2.0,<2.3
numpy>=1.24,<2
scipy>=1.10
scikit-learn==1.3.0
xgboost==2.0.3
catboost==1.2.2
POT==0.9.3
pyyaml>=6.0
tomli>=2.0
tomli-w>=1.0
tqdm>=4.60
optuna==3.5.0
duckdb>=0.10
